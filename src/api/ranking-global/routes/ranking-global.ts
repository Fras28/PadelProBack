// src/api/ranking-global/routes/ranking-global.ts

import { factories } from '@strapi/strapi';

// Este archivo define las rutas CRUD por defecto para el Single Type 'Ranking Global'.
// Las rutas personalizadas se definen en 'custom-ranking-global.ts' en el mismo directorio.
export default factories.createCoreRouter('api::ranking-global.ranking-global');